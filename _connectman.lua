defines function: connectman_slave
  function will attempt to connect to wifi
  in any case where it finds itself not connected

  if it is connected and has no IP, then run timerman





will register a timer to run this function once every five seconds,
and then start the timer (could be made a one liner)