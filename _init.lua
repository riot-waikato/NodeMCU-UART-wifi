function pad : ensures string is at least N long,
               pads right until we get to specified length with 
               specified string (or space if none)


define functions (opensend in own file)




registeruart - this you can still issue commands, but you wont recieve
echo response from the ESP over screen/etc.
puts board in uart mode, runs wifi backend, runs uart manager too







use to disable a running wifi packet send loop




remove identified from packet ("$")
test packet - shouldn't be seen